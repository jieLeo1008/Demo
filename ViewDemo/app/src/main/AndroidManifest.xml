<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.benny.viewdemo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.kotlin.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 仿ZAKER -->
        <activity
            android:name=".ui.java.activity.ZakerGridActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".ui.java.activity.SQLiteActivity">
        </activity>
        <activity android:name=".ui.java.activity.ViewPagerActivity">
        </activity>
        <activity android:name=".ui.java.activity.VideoActivity">
        </activity>
        <activity android:name=".ui.java.activity.NotificationActivity">
        </activity>
        <activity android:name=".ui.java.activity.FingerActivity">
        </activity>
    </application>

</manifest>